# Data

`raw/collections_30k_dataset.zip` is the assignment dataset supplied by the hiring exercise. Extract its CSV files into `data/raw/` before running the notebook/pipeline if you need to execute from scratch.

The repository's analysis uses the supplied schema as-is and explicitly flags dimensions that are absent (Client, Language).
